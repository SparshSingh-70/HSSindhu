package com.cdac.hss.repository;

import com.cdac.hss.entities.WordImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WordImageRepository extends JpaRepository<WordImage, Integer> {
}